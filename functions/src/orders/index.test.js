import { https } from 'firebase-functions'

beforeEach(() => {
  jest.clearAllMocks()
  jest.resetModules()
})

describe("updateOrderStatus", () => {
  it('has a test', () => {
    // TODO: write tests for this.
  })
})
